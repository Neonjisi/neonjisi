/**
 * 환불 소요 안내 — 한 문장으로 고정한다 (spec.md FR-017 · Clarifications Q4 · T002).
 * 확정 문안의 원본(SSOT)은 `specs/004-group-funding/copy.md` §5 다.
 *
 * 미달 · 주최자 취소 · 차액 실패 취소 · 잉여 환불 — **네 경로 전부 같은 말**이어야 하고,
 * `PORTONE_MODE` 가 mock 이든 real 이든 바뀌지 않는다.
 *
 * ⚠️ 이 파일이 존재하는 이유가 곧 사고 사례다. 전에는 같은 문장을 `lib/notification/display.ts`
 *    와 결과 화면이 **각자 적어두고 있었고, 둘이 갈렸다** — 알림은 "영업일 3~5일이 걸릴 수
 *    있어요"(FR-017 원문에서 "기준" 누락), 결과 화면은 "영업일 기준 3~5일 소요됩니다"(어체 불일치).
 *    같은 사용자가 알림 → 결과 화면 순으로 연달아 보는 문장이고 환불 지연 문의의 근거가 되므로,
 *    **문자열을 두 번 적지 않는다.** 고칠 일이 있으면 여기 한 곳만 고친다.
 */

/** FR-017 이 규정한 문장 그대로. 앞 문장에 이어 붙는 것을 전제로 마침표를 포함하지 않는다 */
export const REFUND_NOTICE = '영업일 기준 3~5일이 걸릴 수 있어요'
