# 팀 업무 분담 — 마일스톤 4 (고가 선물을 여럿이 함께 준비한다)

**작성일**: 2026-08-31 · **대상 태스크**: [tasks.md](./tasks.md) 43개 · **팀**: 4명

> [초안 §7](../../docs/2026-08-31-m3-m4-team-assignment.md)(M4 분담 개요)을 T번호로 매핑한
> 확정본이다. **착수는 M3 완료 후** — 분담만 먼저 확정해 둔다. M3 구현 중 설계가 바뀌면
> 이 문서와 004 plan을 함께 손본 뒤 착수한다.

---

## 0. 팀 · 자르는 방향

| 이니셜 | 태스크 수 | M4에서 맡는 것 |
|---|---|---|
| **J** (`Gnuke`) | 16 | 펀딩 스키마·CHECK, **총액 단일 모듈**, 상태 전이, **2단계 예약 동시성**, 개설 서버 |
| **D** (`ReDocu`) | 8 | **정산 전부** — `settleFunding()` 소유(환불·차액·알림 3종), 취소·재시도, funding 알림 |
| **H** (`jisang`) | 16 | **화면 8종 전부** + 홈 섹션 + 개설·참여·내역 E2E |
| **S** (`syjo6658`) | 2 + 상시 | 문구 세트(고지 2종·차액 동의 v1·구분 문구), 3계정 quickstart 검증 |

> D의 번호가 적은 건 일이 적어서가 아니다 — **T014(settle)가 M4에서 가장 무거운 단일
> 태스크**다(M3의 T017 자리). 환불·차액·알림 3종·멱등 잠금이 전부 그 안이다.
>
> M3 규칙 유지: 스토리별 Action 파일 분할 · 마이그레이션 J만 · PortOne은 인터페이스만 경유 ·
> 상태 직접 UPDATE 금지. **M4 신규 규칙: 총액 합산은 `lib/funding/totals.ts` 밖에서 금지.**

```
Phase 1  준비          전원(T001) + S(T002) + D(T003 — 3계정 픽스처 최우선)
Phase 2  기반          J(스키마·총액·전이) + D(settle ★) + J(DAL)     ← T014가 US2·US3을 막는다
Phase 3  US1 개설      J 서버 + H 화면 + S 문구   [P]
Phase 4  US2 참여      J 예약·동시성 + H 화면      ← 캡 동시성이 완료 판정
Phase 5  US3 정산      D 단독                      ← 마일스톤 완료 판정
Phase 6  US4 내역·홈   H 단독                [P — Phase 5와 병행]
Phase 7  마무리        전원
```

---

## 1. 공통 — 전원 (T001)

`.env.local`에 참여자 `E2E_USER4_*` + 비친구 `E2E_USER5_*`(Supabase Add user · Auto Confirm) + `FUNDING_RESERVATION_TTL`.
**펀딩은 3주체 구조라 계정 3개가 필요하다** — 비면 3계정 E2E가 실패 대신 skip (M1·M2와
같은 함정, `skipped` 수 확인).

---

## 2. 팀원별 상세

### 👤 J — 16개

**한 문장으로**: 스키마와 총액·전이·예약의 정확성 층을 세우고, 개설 서버와 참여 동시성을 맡는다.

| Phase | # | 할 일 |
|---|---|---|
| 2 | T004~T005 | 스키마(`Funding`·`FundingContribution`·enum·알림 4종·**Payment FK 회수**) + 마이그레이션 |
| 2 | **T006~T008** | **제약 테스트 먼저 → C9·C10·FK raw SQL → 초록** (M3 C5~C8 계보) |
| 2 | T009~T010 | **총액 단일 모듈** — 판정 `PAID`만 / 캡 `RESERVED`+`PAID` (테스트 먼저). 두 합산이 흩어지면 조용히 깨진다 |
| 2 | T011~T012 | 상태 전이 + 예약 만료 지연 해제 (테스트 먼저) |
| 2 | T016 | `lib/dal/funding.ts` — canViewFunding(수령자의 활성 친구까지) + **지분 3단계 마스킹은 DAL에서 끝낸다** + 모든 조회가 정산·예약 지연 평가 경유 |
| 3 | T018~T019 | 차액 동의(`FUNDING_CONSENT_VERSION` — gift와 독립 버전) + `createFunding`(개설자=수령자면 `min := goal` 강제) |
| 4 | **T024~T025** | **동시 참여 캡 통합 테스트 먼저** → `contributeToFunding` — `FOR UPDATE` 예약 → 결제 → 확정 + **조기 성사 시 settle 즉시 호출** |
| 7 | T039 · T043 | 제약 확인 · lint/build → main |

> 🔑 **T024~T025가 M4의 `PAYING` 잠금 자리다.** 두 명이 동시에 남은 금액을 참여해도 합계가
> 목표를 넘지 않아야 한다 — 예약이 `FOR UPDATE` 안에서 잔여를 선점한다.

### 👤 D — 8개

**한 문장으로**: 정산을 혼자 소유한다 — 환불·차액·알림 3종·멱등이 전부 `settleFunding()` 안이다.

| Phase | # | 할 일 |
|---|---|---|
| 1 | **T003** | **3계정 E2E 픽스처** — 3주체 E2E 전부를 막는 선행 태스크 (M2 T017 자리) |
| 2 | **T013~T014** | **`settleFunding()`** (테스트 먼저) — 판정 멱등 잠금 → 환불(**M3 `refund` 첫 실사용**) → 차액(`chargeBillingKey`) → `SETTLED` → 알림 3종. topup 실패는 성사 유지 + 재시도, 상한 초과 시 취소·전액 환불(clarify Q3) |
| 2 | T015 | `shared.ts` 이식 + proxy matcher + error.tsx |
| 5 | T030~T033 | 정산 E2E 먼저 → `cancelFunding`·`retryFundingTopup` → 알림 4종 목록 매핑 → **초록 = 마일스톤 완료 판정** |

> ⚠️ **T014의 시그니처가 확정되기 전에는 J의 T025(조기 성사 연결)와 D 자신의 T031이 못
> 간다.** contracts §2에 계약이 이미 있다 — Phase 2에서 mock으로 완결.
> `FUNDING_ORGANIZER_TOPUP`은 "동의를 받았어도 고지는 별개다" — 누락되면 T013이 잡는다.

### 👤 H — 16개

**한 문장으로**: 화면 8종과 홈 섹션을 전부 만들고, 3주체 E2E로 지분 공개를 검증한다.

| Phase | # | 할 일 |
|---|---|---|
| 3 | T017 · T020~T022 | 개설 E2E 먼저 → 3스텝/2스텝 화면(SCR-M4-01~03 — **달성선은 숨기지 않고 잠근다**, 차액은 숫자) → 상품 상세 진입점 → 초록 |
| 4 | **T023** | 참여 E2E 먼저 — **지분 3단계 뷰**(3계정) · 잔여 초과 · 비친구 거부 |
| 4 | T026~T029 | 상세(SCR-M4-04 — 진행바=paid · 남은=cap · **"결제 중 N원" 줄** · 게이지 400ms 1회) → 참여(고지 2종 나란히) → 결과 3변형 → 초록 |
| 6 | T034~T038 | 내역 E2E 먼저 → 종료 결과 3변형(차액 주최자 변형 포함) → 펀딩 내역 → **홈 섹션 활성화**(M3에서 숨긴 자리) → 초록 |
| 7 | T040~T041 | 360px 8종 · `'use client'` 예산 4개 · totals 밖 합산 점검 |

> **두 총액의 화면 표기가 이 마일스톤 화면의 핵심이다** — 진행바(결제 완료분)와 남은
> 금액(예약 포함)이 어긋나 보이는 것을 "결제 중 N원" 줄이 해소한다. 값은 전부 DAL이
> 내려준다 — 화면에서 합산·마스킹하지 않는다.

### 👤 S — 2개 + 상시

| # | 할 일 |
|---|---|
| T002 | **문구 세트** (마감: 각 화면 구현 전) — 고지 2종("서연님과 주최자에게 공개됩니다" · "달성선에 못 미치면 전액 환불됩니다") · 차액 동의 v1(최대 부담액 **숫자** 템플릿) · `FAILED`/`CANCELLED` 구분 문구 · 환불 안내 "영업일 3~5일" 고정(clarify Q4) |
| T042 | quickstart V1~V6 **3계정 수동 검증** + `skipped` 수 확인 |
| 상시 | 잔여 캡 동시 참여 시나리오 검증 참관(T024) · M4 결정 5건(F0)의 팀 추인 공유 |

---

## 3. 충돌 지도

| 파일 | 스토리 | 해법 |
|---|---|---|
| `lib/funding/settle.ts` | US2(J가 조기 성사에서 호출)·US3(D 구현) | **D 소유 (T014).** 경계는 contracts §2 — 호출자는 정산 알림·상태에 손대지 않는다 |
| `lib/funding/totals.ts` | 전부 | **J 소유 (T010).** 다른 파일 합산 금지 |
| `app/fundings/actions/contribute.ts` | US2 | **J 소유.** 결제·정산은 인터페이스 호출만 |
| `app/page.tsx` (홈) | US4(T037) | **H 소유** — M3와 동일 |
| `prisma/*` | 전부 | **J만** |
| `components/notification/` | T032 | **D 소유** 유지 (M2·M3 연속) |

## 4. 진행 체크포인트

| 시점 | 확인할 것 |
|---|---|
| Phase 1 끝 | 3계정 로그인 · T002 문구 확정 |
| Phase 2 끝 | **T006 초록**(C9·C10·FK 실재) · **T013 초록**(정산 왕복 + 멱등) |
| Phase 3 끝 | 두 분기 개설 시연 — R3(PRD) 해소가 보인다 |
| Phase 4 끝 | 3계정 참여 + **캡 동시성 통과** — 목표 초과 0건 |
| **Phase 5 끝** | **마일스톤 4 완료 판정** — 성사·미달·취소·차액·환불 전부 |
| Phase 6 끝 | 결과·내역·홈 섹션 |
| Phase 7 끝 | 360px · 제약 3행 · quickstart 3계정 · lint/build → main |
